{
  "name": "everdreamcard",
  "version": "1.0.0",
  "description": "Everdream Digicards — customiser + ordering",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Whispertrinity/EverdreamCard.git"
  },
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "vercel": "^55.0.0"
  }
}
